import { NotificationsPage } from '@/components/notifications/NotificationsPage';

export const metadata = { title: 'Notifications — Stratix' };

export default function NotificationsRoute() {
  return <NotificationsPage />;
}
